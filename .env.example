# Copy to ".env" in the same folder as docker-compose.yml (repo root).
# Never commit the real ".env" file.

OPENROUTER_API_KEY=
# OPENROUTER_MODELS=google/gemini-2.0-flash-lite,openai/gpt-4o-mini

# When you deploy publicly, set your site origin(s), comma-separated:
# CORS_ALLOW_ORIGINS=https://yourdomain.com,http://localhost:8000
